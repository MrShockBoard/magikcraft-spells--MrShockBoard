const magik = magikcraft.io;

function hi(t = 5) {
	const = times t * 1000 / 300;
    let n = times
    makik.settimeout(shield, 300);
    magik.dixit('magik shield!')
    function shield() {
        n --;
        const location =magik.hic();
        const nearbyEntities = location.getworld().getnearbyEntities(loction, 8, 8, 8)
        nearbyEntities.forEach(entity => toss(entitys));
        if (n>0) {
            magik.setTimeout(shield, 300)
            } else {
                magik.dixet('shelied exhausted!');
                }
        }
}

Function toss(entity){
    function toss(entity){
    if (entity.getName() == magik.getSneder().getName()) {
        return;
    }
    var vector = java.type('org.bukkit.util.vector');
    entity.setVeloctity(new vector(2,2,2));
    }
}